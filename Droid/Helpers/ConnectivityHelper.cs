using Android.App;
using Android.Net;

namespace Versus.Droid
{
    public static class ConnectivityHelper
    {
       // public static bool HasConnectivity (Activity activity)
       // {
            //var connectivityManager = (ConnectivityManager)activity.GetSystemService (activity.GetSystemServiceName(ConnectivityService);

            //NetworkInfo activeConnection = connectivityManager.ActiveNetworkInfo;
            //bool isOnline = (activeConnection != null) && activeConnection.IsConnected;
            //return isOnline;
       // }
    }
}

